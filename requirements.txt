# Recipe Q&A Assistant with RAG - Requirements

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Retrieval and text processing
scikit-learn>=1.3.0

# LLM API clients (choose one or both)
anthropic>=0.25.0  # For Claude
openai>=1.0.0      # For GPT models

# Optional: For sentence embeddings (if you want to try embedding-based retrieval)
sentence-transformers>=2.2.0

# Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.25.0

# Evaluation metrics
scipy>=1.11.0

# Visualization (for report)
matplotlib>=3.7.0
seaborn>=0.12.0
